# ux-spec skill

## 作用

把一句话需求引导成一份六层 `spec.md`。

## 引导框架

1. L1 定位与意图：功能是什么、为谁做、边界到哪里。
2. L2 信息架构：页面分哪些区域，每个区域承担什么。
3. L3 核心链路：状态如何流转，用户可以做什么。
4. L4 组件功能细节：每个组件在不同状态下如何表现。
5. L5 边界条件：空、加载、错误、权限不足时怎么办。
6. L6 验收标准：满足哪些条件才算做对。

## 产出

一份可被后续链路读取的 `spec.md`。
