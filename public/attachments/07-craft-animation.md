# craft-animation.md

## 状态反馈

- `<300ms` 不展示加载，避免闪烁。
- `300ms-2s` 使用骨架屏，稳定布局。
- `>2s` 使用 Loader 和进度说明。
- `>10s` 展示超时提示和重试入口。

## 动效约束

- UI 动画不超过 300ms。
- 只动画 transform 和 opacity。
- 每个动画必须说明目的。
- 键盘触发动作不做额外装饰动画。

## 工艺约束

- 品牌色一屏不超过 3 处重点使用。
- 避免等大白卡和无层级网格。
- 中文 UI 默认使用 `var(--font-cn)`。
